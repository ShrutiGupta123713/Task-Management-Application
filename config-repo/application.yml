spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ShrutiGupta123713/Task-Management-Application.git
          clone-on-start: true  # Fetch configs at startup

server:
  port: 8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
